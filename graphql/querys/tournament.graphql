query AllTournamentList {
  allTournaments {
    ...AllTournaments
  }
}

query AllTournamentCalendar {
  allTournaments {
    ...AllTournamentsCalendar
  }
}

query Tournament($id: String!) {
  tournament(id: $id) {
    ...Tournament
  }
}