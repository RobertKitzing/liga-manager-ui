apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-liga-manager-api
  labels:
    {{- include "liga-manager-api.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "liga-manager-api.selectorLabels" . | nindent 4 }}
